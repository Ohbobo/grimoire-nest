import * as mongoose from 'mongoose';

export const BookSchema = new mongoose.Schema({
    id: String,
    // title: String,
    // description: String,
    // author: String,
    // year: Number,
    // genre: String,
    // ratings: [{
    //     userId: String,
    //     grade: Number,
    // }],
    // averageRating: Number,
})